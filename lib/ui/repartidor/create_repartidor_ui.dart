import 'package:flutter/material.dart';

class CreateRepartidorUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      // Text("por que me estas llamando?"),
    );
  }
}